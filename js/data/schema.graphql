type Board {
  id: String
  name: String
}

type profilePicture {
  uri: String
  size: Int
}

type Query {
  user(id: String): User
  board: Board
}

type User {
  id: String
  name: String
  profilePhoto(size: Int): profilePicture
}
