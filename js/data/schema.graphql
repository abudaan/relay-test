schema {
  query: Query
}

type profilePicture {
  uri: String
  size: Int
}

type Query {
  users: Users
  user(userId: Int): User
}

type User {
  userId: Int
  name: String
  profilePhoto(size: Int): profilePicture
}

type Users {
  users: [User]
}
